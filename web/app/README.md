# Web App

This repository uses server-rendered placeholder pages for:

- `/app`
- `/app/mcp-connect`

The route-level source for MCP connection guidance is represented by `web/app/routes/mcp-connect`.
